import { c } from '@dothis/dto';

// import ZZZTEST from './test';

const Page = () => {
  const randomChoice = Math.random() < 0.5 ? 'news' : 'youtube';

  return <>{/* <ZZZTEST randomChoice={randomChoice} /> */}</>;
};

export default Page;
